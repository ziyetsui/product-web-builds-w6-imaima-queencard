# 0010 验收标准

- [ ] 评测矩阵跑完 ≥3 个候选模型，打分表和样张归档在本 feature `docs/`
- [ ] 至少 1 个新模型正式接入：配置化新增（不改前端组件代码）
- [ ] 新模型在带参考图场景下走各自的 edit 能力路径，风格锁定度达到可用线
- [ ] 每个模型的积分成本已配置，估价接口（estimate）返回正确
- [ ] 模型不可用时降级明确：报错文案清晰，不吞积分（hold 正确释放）
- [ ] `pnpm vitest run` 相关测试全绿；`pnpm build` 成功
