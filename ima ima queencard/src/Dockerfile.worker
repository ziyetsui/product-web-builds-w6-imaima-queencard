FROM node:22-slim

WORKDIR /app

COPY package.json pnpm-lock.yaml ./
RUN npm install -g pnpm@10.33.0 \
  && pnpm install --frozen-lockfile

COPY tsconfig.json ./
COPY src ./src

ENV NODE_ENV=production
ENV WORKER_HEALTH_PORT=8081

EXPOSE 8081

USER node

CMD ["pnpm", "worker:generation"]
